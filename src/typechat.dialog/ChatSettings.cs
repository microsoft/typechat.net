// Copyright (c) Microsoft. All rights reserved.

namespace Microsoft.TypeChat;

/*
public class ChatSettings
{
    string[] _instructions;
    List<Message> _context;
    RequestSettings? _requestSettings;

    public ChatSettings()
    {
        _context = new List<Message>();
        _requestSettings = new RequestSettings();
    }

    public string[]? Instructions { get; set; }
    public IList<Message> FixedContext => _context;
    public RequestSettings RequestSettings => _requestSettings;

    public int MaxRequestLength { get; set; } = 2000;
    public bool HasContext => (_context.Count > 0);
    public bool HasInstructions => (!Instructions.IsNullOrEmpty());
}
*/
